# =============================================================================
# BEDROCK 2.0 - RASPBERRY PI 5 REQUIREMENTS
# Исправленные зависимости с учетом всех известных проблем Pi 5
# =============================================================================

# =============================================================================
# CRITICAL FIXES FOR RASPBERRY PI 5
# =============================================================================

# CORE GUI FRAMEWORK
# ==================
# Используем проверенную стабильную версию Kivy
kivy>=2.1.0,<2.4.0

# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: KivyMD 2.0.0 НЕ СУЩЕСТВУЕТ в PyPI!
# Проблема была в попытке установить несуществующую версию
# Используем стабильную KivyMD 1.1.1 которая точно работает
kivymd>=1.1.1,<2.0.0

# RASPBERRY PI 5 GPIO SUPPORT
# ===========================
# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: RPi.GPIO НЕ РАБОТАЕТ на Pi 5
# Ошибка "No module named 'lgpio'" решается установкой rpi-lgpio
rpi-lgpio>=0.6.0

# Альтернативная GPIO библиотека (резервная)
gpiozero>=1.6.2

# GRAPHICS AND MULTIMEDIA
# =======================
# Стабильная версия Pillow для обработки изображений
pillow>=9.5.0,<11.0.0

# Pygame для аудио поддержки - версия совместимая с Pi 5
pygame>=2.5.0,<2.6.0

# HTTP REQUESTS
# =============
requests>=2.28.0,<3.0.0

# =============================================================================
# SENSOR LIBRARIES FOR PI 5
# =============================================================================

# Adafruit CircuitPython для Pi 5
adafruit-blinka>=8.20.0

# Сенсоры температуры и влажности (AHT21/AHT20)
adafruit-circuitpython-ahtx0>=1.0.18

# Сенсор качества воздуха (ENS160)
adafruit-circuitpython-ens160>=1.0.9

# I2C коммуникация
smbus2>=0.4.2

# =============================================================================
# AUDIO SUPPORT (КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ)
# =============================================================================

# PyGObject для GStreamer поддержки (если доступно)
# Устанавливается из apt для лучшей совместимости с ARM64
# pygobject>=3.42.0

# =============================================================================
# DEVELOPMENT AND DEBUGGING (ОПЦИОНАЛЬНО)
# =============================================================================

# Логирование и отладка
# colorlog>=6.7.0

# =============================================================================
# СИСТЕМНЫЕ ПАКЕТЫ ДЛЯ УСТАНОВКИ ЧЕРЕЗ APT (НЕ PIP)
# =============================================================================
# Эти пакеты должны устанавливаться через apt для лучшей ARM64 совместимости:
#
# ОБЯЗАТЕЛЬНЫЕ:
# sudo apt install -y python3-lgpio python3-gpiozero 
# sudo apt install -y python3-gi python3-gi-cairo gir1.2-gtk-3.0
# sudo apt install -y python3-numpy python3-scipy
# sudo apt install -y i2c-tools python3-smbus python3-smbus2
#
# АУДИО:
# sudo apt install -y python3-pygame (если pip версия не работает)
# sudo apt install -y alsa-utils pulseaudio pulseaudio-utils
# sudo apt install -y gstreamer1.0-tools gstreamer1.0-plugins-*
#
# ГРАФИКА:
# sudo apt install -y libgl1-mesa-dev libgles2-mesa-dev
# sudo apt install -y python3-opengl
#
# Скрипт деплоя автоматически устанавливает эти пакеты
# =============================================================================

# =============================================================================
# ВЕРСИОННЫЕ ОГРАНИЧЕНИЯ ДЛЯ СТАБИЛЬНОСТИ
# =============================================================================

# Ограничиваем версии для избежания конфликтов
setuptools>=65.0.0,<70.0.0
wheel>=0.38.0,<1.0.0
pip>=22.0.0,<24.0.0

# Зависимости Kivy/KivyMD
docutils>=0.18.0,<1.0.0
pygments>=2.14.0,<3.0.0
certifi>=2022.12.7

# =============================================================================
# ИЗВЕСТНЫЕ ИСПРАВЛЕНИЯ ОШИБОК
# =============================================================================

# 1. KivyMD 2.0.0 - НЕ СУЩЕСТВУЕТ на PyPI
#    Решение: kivymd>=1.1.1,<2.0.0

# 2. "No module named 'lgpio'" на Pi 5
#    Решение: rpi-lgpio>=0.6.0 + системный python3-lgpio

# 3. PyGObject/Cairo ошибки компиляции
#    Решение: Установка через apt вместо pip

# 4. Kivy не найден после установки
#    Решение: Правильный порядок установки зависимостей

# 5. i2cdetect не найден
#    Решение: sudo apt install i2c-tools

# 6. Проблемы с аудио на Pi 5
#    Решение: Системные ALSA пакеты + правильная конфигурация

# =============================================================================
# ПОРЯДОК УСТАНОВКИ (КРИТИЧНО!)
# =============================================================================

# 1. Системные пакеты через apt (скрипт деплоя делает это автоматически)
# 2. Создание venv с --system-site-packages
# 3. Обновление pip, setuptools, wheel
# 4. Установка этого requirements.txt

# =============================================================================
# ПРОВЕРКА ПОСЛЕ УСТАНОВКИ
# =============================================================================

# Проверить критические импорты:
# python -c "import kivy; print(f'Kivy: {kivy.__version__}')"
# python -c "import kivymd; print(f'KivyMD: {kivymd.__version__}')"
# python -c "import pygame; print(f'Pygame: {pygame.version.ver}')"
# python -c "import lgpio; print('GPIO: OK')"
# python -c "import board, busio; print('Sensors: OK')"